#ifndef SCHED_H
#define SCHED_H

#define CLONE_VM            (1UL << 0)    /* shared virtual memory between processes */
#define CLONE_FS            (1UL << 1)    /* shared fs info between processes */
#define CLONE_SIGNAL        (1UL << 2)    /* shared signal between process */

#endif